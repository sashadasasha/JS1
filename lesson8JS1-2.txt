Задание 1.

Если ориентироваться на то, что Вы показали на уроке, то, мне кажется, через замыкание можно реализовать функционал подсчета 
количества товаров в корзине. Возможно, что-то еще, но это первое, что пришло в голову) Надеюсь попрактиковаться удастся позднее, потому что
вроде бы ничего сложного в замыкании, но как-то в голове тяжело уложить)

Задание 2.

if (!("a" in window)) {
      var a = 1;
}
alert(a); - в window у нас будет а, т.к. мы ее инициализировали с помощью var, но значение у нее будет только внутри условия if, поэтому 
в alert будет undefined. Если мы инициализируем а с помощью let, то будет ошибка, т.к. не только значение будет внутри if, но и вся переменная будет инициализирована только внутри условия.

var b = function a(x) {
    x && a(--x);
};
alert(a); - вообще какая-то неверная, на мой взгляд запись. Надо было либо просто объявлять функцию без  присваивания к переменной b, либо
делать function expression, и в переменную b класть анонимную функцию. Так возникнет ошибка.

function a(x) {
    return x * 2;
}
var a;
alert(a(2)); - здесь просто выведется сама функция, ее синтаксис; если бы мы сделали alert a(2), например, то вычислился бы результат, где x =2. строчка 'var a' меня тут только путает. Зачем еще инициализировать переменную с таким же именем, как и функция? Хотя, если присвоить ей значение, то выведется именно оно. Не очень, если честно, понимаю как это работает) Наверно, переменная не должна называться так же, как и имеющая имя функция, а то возникает путаница.


function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3); - это показалось самым логичным и простым - вызываем функцию, где передаем через аргументы значения x, y, a. Функция из аргументов выбирает тот, что лежит под индексом [2], т.е третий по счету, т.е. аргумент а, присваивает ему значение 10 и выводит а, соответственно выведется число 10.


function a() {
    alert(this);
}
a.call(10); - тут не знала совсем, запустила получила [object Window], в MDN прочитала это - значения null и undefined будут заменены глобальным объектом. 

